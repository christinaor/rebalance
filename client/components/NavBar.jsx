import React, {useEffect, useState} from "react";
import { useNavigate, Routes, Route, Navigate } from 'react-router-dom';

const NavBar = (props) => {
  return (
    <div>Initial NavBar Component</div>
  )
};

export default NavBar;